server {
  listen 80;
#   server_name DNS -URL-.com;

  location / {
    proxy_pass http://127.0.0.1:8080;   # frontend 컨테이너로
  }

  location /api {
    proxy_pass http://127.0.0.1:3000;   # backend 컨테이너로
  }
}